# Site copy (CROATOA live)

Bio and tracklist for [`sites/croatoa-live`](../) are stored here so the Vercel build stays self-contained (no reads outside this app folder).

When you update the root [`../../bio.txt`](../../bio.txt) or [`../../tracklist.txt`](../../tracklist.txt), mirror the changes into `bio.txt` / `tracklist.txt` in this folder (or edit here only if this site is the source of truth).
